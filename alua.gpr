project ALua is

   for Languages use ("Ada", "C");
   for Source_Dirs use (".", "tests", "examples");
   for Object_Dir use "build/";
   for Exec_Dir use "exec/";
   for Main use ("simple_example.adb", "functions_example.adb");

   package Ide is
      for Vcs_Kind use "Git";
      for Documentation_Dir use "doc";
   end Ide;

   package Naming is
      for Body_Suffix ("project file") use ".gpr";
   end Naming;

   package Builder is
      for Default_Switches ("ada") use ("-j2", "-g");
   end Builder;

   package Compiler is
      for Default_Switches ("ada") use ("-gnat12", "-gnata", "-gnato", "-g", "-fstack-check", "-gnatf", "-gnatwZ");
   end Compiler;

   package Linker is
      for Default_Switches ("ada") use ("-g", "-llua", "-lm");
   end Linker;

end ALua;
